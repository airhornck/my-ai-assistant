# 本地开发环境变量示例
# 复制为 .env 或 .env.dev 使用（应用会按 .env / .env.dev / .env.prod 顺序加载第一个存在的文件）
# 与 docker-compose.dev.yml 配套：先 docker compose -f docker-compose.dev.yml up -d，再 uvicorn main:app --reload --port 8000

# ===== 数据库配置（与 docker-compose.dev.yml 一致）=====
DATABASE_URL=postgresql+asyncpg://postgres:MyDbPass123@localhost:5432/ai_assistant
POSTGRES_USER=postgres
POSTGRES_PASSWORD=MyDbPass123
POSTGRES_DB=ai_assistant
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# ===== Redis 配置 =====
REDIS_URL=redis://localhost:6379/0

# ===== LLM 类接口 Key =====
DASHSCOPE_API_KEY=sk-your_dashscope_key_here

# ===== 搜索类接口 Key（可选）=====
SEARCH_PROVIDER=baidu
# BAIDU_SEARCH_API_KEY=bce-v3/your_key

# ===== 服务端口 =====
PORT=8000
DEBUG=True
