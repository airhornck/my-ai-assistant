# 生产环境变量示例。复制为 .env.prod 并修改后使用：
#   cp .env.prod.example .env.prod
# 启动: docker compose --env-file .env.prod -f docker-compose.prod.yml up -d

POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_prod
POSTGRES_DB=ai_assistant

# 主应用（分析/生成/评估）与记忆优化器（memory-optimizer）均使用 DeepSeek，必填
DEEPSEEK_API_KEY=sk-cef65d7e728d43d79a4a23d642faa6d0
REDIS_URL=redis://redis:6379/0
# ！！！注意：数据库URL必须是异步格式，以 postgresql+asyncpg:// 开头
DATABASE_URL=postgresql+asyncpg://postgres:change_me_prod@postgres:5432/ai_assistant

PYTHONUNBUFFERED=1
PYTHONPATH=/app